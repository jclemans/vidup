<% if @video.new_record? %>
  alert('The video could not be uploaded: <%= j @video.errors.full_messages.join(', ').html_safe %>');
<% else %>
  $('#new-videos').append('<%= j render @video %>');
<% end %>
