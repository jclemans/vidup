<% if @video.new_record? %>
  $('.alert-info').append("Failed to upload: <%= j @video.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $('#latest-video').prepend('<%= j render @video %>');
<% end %>
